export default function DeckBuilder() {
  return (
    <section style={{ padding: 24 }}>
      <h1>Deck Builder</h1>
      <p>
        Skeleton — halaman Deck Builder. UI pemilihan 30 kartu + 6 fusion
        (validasi 30/6) akan di-port di Fase 6.4, terhubung ke backend
        <code> POST /decks</code> di Fase 6.6.
      </p>
    </section>
  )
}
