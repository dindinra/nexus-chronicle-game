export default function GameBoard() {
  return (
    <section style={{ padding: 24 }}>
      <h1>Game Board</h1>
      <p>
        Skeleton — halaman Game Board (arena). Logic game engine (turn, combat,
        fusion, AI, efek kartu) akan di-port dari <code>_legacy-reference/index.html</code>{' '}
        mulai Fase 6.7.
      </p>
    </section>
  )
}
